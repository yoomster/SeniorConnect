@page "/Activities"
@model SeniorConnect.WebApp.Pages.ActivityPages.AllActivitiesListedModel
@{
    ViewData["Title"] = "All Activities";
}

<h1>@ViewData["Title"]</h1>

@*   
@if (Model.AllActivities!= null && Model.AllActivities.Count > 0)
 {
     <table class="table">
         <thead>
             <tr>
                 <th>Name</th>
                 <th>Description</th>
                 <th>Date</th>
                 <th>Start time</th>
                 <th>End time</th>
             </tr>
         </thead>
         <tbody>
            @foreach (var activity in Model.AllActivities)
             {
                 <tr>
                    <td>@activity.Name</td>
                    <td>@activity.Description</td>
                    <td>@activity.Date</td>
                    <td>@activity.StartTime</td>
                    <td>@activity.EndTime</td>
                 </tr>
             }
         </tbody>
     </table>
 } 
 else
 {
     <p>No users found.</p> 
 } *@
